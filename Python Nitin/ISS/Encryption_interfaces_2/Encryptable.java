public interface Encryptable {
    String encrypt(String stringToEncrypt);
    String decrypy(String stringData);
}
class AES implements Encryptable {
    private String AES_ALGORITHM = "AES";
    private
}